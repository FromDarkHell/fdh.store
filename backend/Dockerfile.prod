FROM node:20

WORKDIR /app

COPY package*.json ./
RUN npm install --production

COPY . .

# Build Medusa (if applicable)
RUN npm run build || echo "No build step for Medusa"

EXPOSE 9000
CMD ["npm", "run", "start"]
